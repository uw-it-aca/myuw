version: '3'

services:
  web:
    build: .
    container_name: myuw_web
    volumes:
      - ./myuw:/app/myuw
    ports:
      - "8000:8000"
    environment:
      REMOTE_USER: 'javerage'
      PORT: ${PORT:-8000}
    stdin_open: true
    tty: true
